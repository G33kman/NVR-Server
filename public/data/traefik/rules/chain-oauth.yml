http:
  middlewares:
    chain-oauth:
      chain:
        middlewares:
          # - middlewares-traefik-bouncer
          - middlewares-rate-limit
          - middlewares-secure-headers
          - middlewares-oauth